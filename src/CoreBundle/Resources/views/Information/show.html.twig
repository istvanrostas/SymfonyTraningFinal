{% extends "CoreBundle::layout.html.twig" %}

{% block title %}{{ info.title }}{% endblock %}

{% block content %}

    <div class="content">
        <h2 class="heading">{{ info.title }}</h2>
        <p>
            {{ info.description }}
        </p>
    </div>

{% endblock %}